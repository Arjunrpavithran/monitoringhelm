apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitoringhelm.fullname" . }}
  labels:
    {{- include "monitoringhelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monitoringhelm.selectorLabels" . | nindent 4 }}
